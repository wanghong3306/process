#!/usr/bin/env php
<?php

require __DIR__ . '/../vendor/autoload.php';

if (isset($argv[1])) {
    $userFile = $argv[1];
    if (is_readable($userFile)) {
        include $userFile;
    }
}

